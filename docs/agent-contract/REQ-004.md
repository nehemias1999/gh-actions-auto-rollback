# Agent Contract: REQ-004

## Spec Reference
- **Change**: `automated-rollback-pipeline`
- **Requirement**: `REQ-004-deploy-workflow.md`
- **Spec file**: `openspec/changes/automated-rollback-pipeline/requirements/REQ-004-deploy-workflow.md`
- **Tasks**: 3.1 - 3.2, 4.1 in `openspec/changes/automated-rollback-pipeline/tasks.md`

## PERMITTED Files (exact list)
- `.github/workflows/deploy-prod.yml`

## NUNCA (never touch)
- `openspec/` — specs are read-only for implementer
- `scripts/deploy.sh` — owned by REQ-007 (this workflow only CALLS it; assume interface `./scripts/deploy.sh --version <semver>`)
- `scripts/healthcheck.sh`, `scripts/rollback.sh` — owned by REQ-005/REQ-006
- `tests/` — bats tests owned by REQ-007/REQ-008
- Python app source (`src/`), CI workflow (`.github/workflows/ci.yml`)
- Root `README.md`
- Any file not in PERMITTED list

## Interface / API Contract
- **Deploy Workflow** in `.github/workflows/deploy-prod.yml`
- **Trigger**: `push` to `main` branch
- **Steps**:
  1. Checkout + setup build env (Docker buildx).
  2. **semantic-release** (angular preset, conventional commits) determines next version; expose as step/job output `version`.
  3. Build Docker image, tag with semantic version (e.g. `v1.2.3`) AND `latest` (`docker/build-push-action`, local load or registry per available creds — prefer load-only if no registry configured).
  4. Call `./scripts/deploy.sh --version <version>`; workflow FAILS if the script exits non-zero (deploy failure path). Propagate health/rollback behavior to the script (implemented in REQ-005/006/007).
  5. **Output** the deployed version (job/workflow output `deployed-version`) for rollback reference (AC-008).
- **End-to-end deploy success/failure behavior** (AC-005/AC-006/AC-007) is integration-verified in REQ-008; here only wire the invocation + failure propagation + version output.
- **Validation**: `yamllint .github/workflows/deploy-prod.yml` passes.

## Project Profile: Pipeline/CI-CD
**Verification commands** (run from worktree root):
```bash
# Validate workflow syntax
yamllint .github/workflows/deploy-prod.yml

# Test locally with act (if available; requires local Docker)
act push --workflows .github/workflows/deploy-prod.yml --secret-file .secrets.local
```

## Definition of Done (verifiable)
- [ ] `.github/workflows/deploy-prod.yml` exists
- [ ] Triggers on `push` to `main`
- [ ] semantic-release step determines version from conventional commits
- [ ] Docker build step tags image with semantic version AND `latest`
- [ ] Calls `./scripts/deploy.sh --version <version>`; non-zero exit fails the workflow
- [ ] Exposes `deployed-version` output for rollback reference
- [ ] `yamllint .github/workflows/deploy-prod.yml` passes
- [ ] Code-doc-standard compliant header on deploy-prod.yml

## Implementer Report Format
Return `DONE|BLOCKED|NEEDS_CONTEXT` with:
- **Evidence**: paste actual command outputs (yamllint, act test if possible)
- **Files changed**: list of created/modified files
- **Blockers**: if any, specific gap description
